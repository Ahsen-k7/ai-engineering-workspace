# Week 1 — Day 2: OOP Task

Simple `BankAccount` model demonstrating:
- `__init__` with initial balance
- `deposit()` and `withdraw()` with validation
- `get_balance()` method
- Proper exception handling

Run tests from repo root:
```bash
pytest -v